#include <stdio.h>
#include "xor_ann.h"
#include "nnetwork.h"
//this file was not autogenerated

int main() {
    InitNetwork();
    for(int i = 0; i <= 1; i++) {
        for(int j = 0; j <= 1; j++) {
            for(int k = 0; k <= 1; k++) {
                for(int l = 0; l <= 1; l++) {
                    xor_ann_I_x1((float)i);
                    xor_ann_I_x2((float)j);
                    xor_ann_I_x3((float)k);
                    xor_ann_I_x4((float)l);
                    printf("Input: %d, %d, %d, %d\tShould be: %d", i, j, k, l, (i ^ j) ^ (k ^ l));
                    xor_ann();
                }
            }
        }
    }
    
    //
}

void xor_ann_O_y(float y) {
    printf("\tOutput: %f\r\n", y);
}